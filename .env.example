# Optional. The agent answers with a deterministic local model out of the box —
# every acceptance item is reachable without any key. If you want live LLM
# answers, reply to your invite email and we'll send a scoped, spend-capped key.
# Then: cp .env.example .env, paste it below, and `make up` again.
ANTHROPIC_API_KEY=
